# we first need to install the requests package by typing
# in the terminal: pip install requests

# now we can use requests in our program
import requests
response = requests.get("https://example.com")